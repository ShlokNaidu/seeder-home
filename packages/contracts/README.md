# contracts

## Responsibility
The single source of truth for all internal typescript definitions, schemas, and strictly enforced interfaces across the monorepo.

## Public API
Type exports: `MutationSurface`, `ActionTrace`, `CapabilityProfile`, `VALIDATE_MODE_POLICY`.

## Dependencies
None
