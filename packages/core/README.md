# core

## Responsibility
Shared business logic, rate limiters, and session state tracking utilities.

## Public API
Classes: `SessionManager`, `RateLimiter`, `FeatureRegistry`.

## Dependencies
None
