# browser-runtime

## Responsibility
Provides a generic headless browser execution context leveraging Playwright for unopinionated DOM traversal.

## Public API
`BrowserRuntime` class exporting `launch()`, `navigate()`, `getPage()`, `shutdown()`.

## Dependencies
`playwright`
