# test-app

## Responsibility
A local React-based CRM application simulating a SaaS target for offline validation and end-to-end benchmarking.

## Public API
Internal HTTP Server accessible via `npm run dev` at `http://localhost:3000`.

## Dependencies
React, Vite.
