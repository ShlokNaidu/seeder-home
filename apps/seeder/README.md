# seeder

## Responsibility
The monolithic orchestration CLI for executing validation sequences, discovering topology, and generating semantic data models.

## Public API
CLI-based orchestration: `pnpm validate --target <target>`

## Dependencies
Depends on all `@seeder/*` packages (contracts, browser-runtime, etc).
