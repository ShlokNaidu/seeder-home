# Task 0006: Session Acquisition Engine

## Objective
Implement the first engine to log into `test-app` using `browser-runtime`.

## Acceptance Criteria
- Engine successfully logs into `test-app`.
- Returns an `AuthenticatedBrowserSession` model.
- Integration tests pass.
