# Task 0001: Bootstrap Monorepo

## Objective
Verify the pnpm + Turborepo structure and ensure all packages cross-link successfully.

## Acceptance Criteria
- `pnpm install` succeeds.
- `turbo run build` succeeds across all packages.
- TypeScript references resolve correctly.
