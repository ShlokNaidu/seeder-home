# Task 0005: Browser Runtime Abstraction

## Objective
Implement the Playwright lifecycle wrappers in `packages/browser-runtime`.

## Acceptance Criteria
- Encapsulates `Browser` and `BrowserContext`.
- No other package imports Playwright directly.
- Compiles and passes unit tests.
